---
- name: Update packages on remote host
  hosts: localhost
  become: true
  gather_facts: false
  
  

  tasks:
    - name: Run yum update
      dnf:
        name: '*'
        state: latest
      become: true
